{
  "name": "hackpay",
  "version": "1.0.0",
  "description": "HackPay — agentic dual-control hackathon prize escrow in INR via Razorpay",
  "type": "module",
  "scripts": {
    "dev": "npm run --prefix frontend dev",
    "dev:web": "npm run --prefix frontend dev",
    "build": "tsc --noEmit",
    "build:web": "npm run --prefix frontend build"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.0.0"
  }
}
